<!--
    Copyright (c) Microsoft. All rights reserved.
    This code is licensed under the MIT License (MIT).
    THE CODE IS PROVIDED “AS IS”, WITHOUT WARRANTY OF ANY KIND, EXPRESS OR IMPLIED,
    INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
    FITNESS FOR A PARTICULAR PURPOSE AND NONINFRINGEMENT.
    IN NO EVENT SHALL THE AUTHORS OR COPYRIGHT HOLDERS BE LIABLE FOR ANY CLAIM,
    DAMAGES OR OTHER LIABILITY, WHETHER IN AN ACTION OF CONTRACT,
    TORT OR OTHERWISE, ARISING FROM, OUT OF OR IN CONNECTION WITH
    THE CODE OR THE USE OR OTHER DEALINGS IN THE CODE.
    
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.
-->

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:system="clr-namespace:System;assembly=System.Runtime">

    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <system:Double x:Key="DataGridRowHeaderMinWidth">24</system:Double>
    <system:Double x:Key="DataGridCellMinHeight">32</system:Double>
    <system:Double x:Key="DataGridColumnHeaderMinHeight">32</system:Double>
    <system:Double x:Key="DataGridSortIndicatorIconFontSize">8</system:Double>
    <system:Double x:Key="DataGridSelectAllIconFontSize">6</system:Double>
    <Thickness x:Key="DataGridSortIndicatorIconMargin">4,4,2,4</Thickness>
    <Thickness x:Key="DataGridCellBorderThickness">1</Thickness>
    <Thickness x:Key="DataGridCellFocusVisualBorderThicness">3</Thickness>
    <Thickness x:Key="DataGridColumnHeaderPadding">4,0</Thickness>
    <Thickness x:Key="DataGridBorderThickness">1</Thickness>
    <CornerRadius x:Key="DataGridHeaderDropSeparatorCornerRadius">2</CornerRadius>
    <Thickness x:Key="DataGridHeaderDropSeparatorPadding">0 4</Thickness>
    <system:Double x:Key="DataGridHeaderDropSeparatorWidth">4</system:Double>
    <Thickness x:Key="DataGridColumnFloatingHeaderBorderThickness">1</Thickness>

    <SolidColorBrush x:Key="{x:Static DataGrid.FocusBorderBrushKey}"
        Color="{DynamicResource KeyboardFocusBorderColor}" />

    <!-- DataGrid Helper Styles -->

    <Style x:Key="ColumnHeaderGripperStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Width" Value="8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="SizeWE" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RowHeaderGripperStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Height" Value="8" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="SizeNS" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="DataGridRowValidationErrorTemplate">
        <TextBlock Margin="2,0,0,0"
            VerticalAlignment="Center"
            Foreground="{DynamicResource DataGridRowValidationErrorForeground}"
            FontFamily="{DynamicResource SymbolThemeFontFamily}"
            Text="&#xEB90;" />
    </ControlTemplate>

    <Style x:Key="DataGridCellFocusVisualStyle">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        BorderBrush="{DynamicResource DataGridCellFocusVisualBorderBrush}"
                        BorderThickness="{DynamicResource DataGridCellFocusVisualBorderThicness}"
                        CornerRadius="{DynamicResource ControlCornerRadius}"
                        Margin="-1"
                        Panel.ZIndex="1"
                        SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataGrid Control Styles -->

    <Style x:Key="DefaultDataGridColumnFloatingHeaderStyle" TargetType="{x:Type Control}">
        <Setter Property="Opacity" Value="{DynamicResource DataGridColumnFloatingHeaderOpacity}" />
        <Setter Property="BorderBrush"
            Value="{DynamicResource DataGridColumnFloatingHeaderBorderBrush}" />
        <Setter Property="BorderThickness"
            Value="{DynamicResource DataGridColumnFloatingHeaderBorderThickness}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Control}">
                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}">
                        <Grid>
                            <Canvas Name="PART_VisualBrushCanvas" Margin="{TemplateBinding Margin}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridHeaderDropSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Background"
            Value="{DynamicResource DataGridHeaderDropSeparatorBackground}" />
        <Setter Property="Width" Value="{DynamicResource DataGridHeaderDropSeparatorWidth}" />
        <Setter Property="Padding" Value="{DynamicResource DataGridHeaderDropSeparatorPadding}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Margin="{TemplateBinding Padding}"
                        Width="{TemplateBinding Width}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource DataGridHeaderDropSeparatorCornerRadius}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="DataGridScrollViewerTemplate" TargetType="{x:Type ScrollViewer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--Left Column Header Corner -->
            <Button Command="{x:Static DataGrid.SelectAllCommand}"
                Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=CellsPanelHorizontalOffset}"
                Style="{DynamicResource {ComponentResourceKey TypeInTargetAssembly={x:Type DataGrid}, ResourceId=DataGridSelectAllButtonStyle}}"
                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.All}}"
                Focusable="False" />

            <!--Column Headers-->
            <DataGridColumnHeadersPresenter Grid.Column="1"
                Name="PART_ColumnHeadersPresenter"
                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.Column}}" />

            <!--DataGrid content-->
            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                Grid.Row="1" Grid.ColumnSpan="2"
                CanContentScroll="{TemplateBinding CanContentScroll}" />

            <ScrollBar Name="PART_VerticalScrollBar"
                Grid.Row="1" Grid.Column="2"
                Orientation="Vertical"
                Maximum="{TemplateBinding ScrollableHeight}"
                ViewportSize="{TemplateBinding ViewportHeight}"
                Value="{Binding Path=VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

            <Grid Grid.Row="2" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=NonFrozenColumnsViewportHorizontalOffset}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ScrollBar Name="PART_HorizontalScrollBar"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Maximum="{TemplateBinding ScrollableWidth}"
                    ViewportSize="{TemplateBinding ViewportWidth}"
                    Value="{Binding Path=HorizontalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                    Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style x:Key="DefaultDataGridStyle" TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{DynamicResource DataGridBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridBorderBrush}" />
        <Setter Property="Border.CornerRadius" Value="4" />
        <Setter Property="BorderThickness" Value="{DynamicResource DataGridBorderThickness}" />
        <Setter Property="Foreground" Value="{DynamicResource DataGridForeground}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGridLinesBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DataGridLinesBrush}" />
        <Setter Property="DropLocationIndicatorStyle"
            Value="{StaticResource DefaultDataGridHeaderDropSeparatorStyle}" />
        <Setter Property="DragIndicatorStyle"
            Value="{StaticResource DefaultDataGridColumnFloatingHeaderStyle}" />
        <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGrid}">
                    <Border x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="DG_ScrollViewer"
                            Template="{StaticResource DataGridScrollViewerTemplate}">
                            <ItemsPresenter
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsGrouping" Value="True" />
                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="False" />
                </MultiTrigger.Conditions>
                <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DefaultDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="{DynamicResource DataGridRowBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridRowBorderBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridRowBorderThickness}" />
        <Setter Property="Border.CornerRadius" Value="4" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="ValidationErrorTemplate"
            Value="{StaticResource DataGridRowValidationErrorTemplate}" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Border x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        SnapsToDevicePixels="True">
                        <SelectiveScrollingGrid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <DataGridCellsPresenter Grid.Column="1"
                                ItemsPanel="{TemplateBinding ItemsPanel}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                            <DataGridDetailsPresenter
                                SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=AreRowDetailsFrozen, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}}"
                                Grid.Column="1" Grid.Row="1"
                                Visibility="{TemplateBinding DetailsVisibility}" />

                            <DataGridRowHeader
                                SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                Grid.RowSpan="2"
                                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.Row}}" />
                        </SelectiveScrollingGrid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridRowBackgroundPointerOver}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridRowBorderBrushPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridRowBackgroundSelected}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridRowBorderBrushSelected}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsNewItem" Value="True">
                <Setter Property="Margin"
                    Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=NewItemMargin}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DefaultDataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="{DynamicResource DataGridCellBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridCellBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource DataGridCellBorderThickness}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource DataGridCellFocusVisualStyle}" />
        <Setter Property="MinHeight" Value="{StaticResource DataGridCellMinHeight}" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Border x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            VerticalAlignment="Stretch"
                            Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridCellBackgroundPointerOver}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridCellBorderBrushPointerOver}" />
                            <Setter Property="Foreground"
                                Value="{DynamicResource DataGridCellForegroundPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridCellBackgroundSelected}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridCellBorderBrushSelected}" />
                            <Setter Property="Foreground"
                                Value="{DynamicResource DataGridCellForegroundSelected}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="Selector.IsSelectionActive" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridCellBackgroundInactiveSelection}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridCellBorderBrushInactiveSelection}" />
                        </MultiTrigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridCellBackgroundFocused}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridCellBorderBrushFocused}" />
                            <Setter Property="Foreground"
                                Value="{DynamicResource DataGridCellForegroundFocused}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource {x:Static DataGrid.FocusBorderBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"
                                Value="{DynamicResource DataGridCellForegroundDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridRowHeaderStyle" TargetType="{x:Type DataGridRowHeader}">
        <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridHeaderBorderBrush}" />
        <Setter Property="MinWidth" Value="{StaticResource DataGridRowHeaderMinWidth}" />
        <Setter Property="BorderThickness" Value="0 0 1 0" />
        <Setter Property="SeparatorVisibility" Value="Visible" />
        <Setter Property="SeparatorBrush" Value="{DynamicResource DataGridHeaderSeparatorBrush}" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border x:Name="HeaderBorder"
                            Grid.ColumnSpan="2"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" />

                        <Border x:Name="SeparatorBorder"
                            Grid.ColumnSpan="2"
                            BorderBrush="{TemplateBinding SeparatorBrush}"
                            Visibility="{TemplateBinding SeparatorVisibility}"
                            BorderThickness="0 0 0 1" />

                        <Rectangle
                            x:Name="ActiveRectangle"
                            Width="3"
                            Height="18"
                            Margin="2,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Fill="{DynamicResource DataGridPillFillBrush}"
                            RadiusX="2"
                            RadiusY="2"
                            Visibility="Hidden" />

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ContentPresenter
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True" />
                            <Control SnapsToDevicePixels="False"
                                VerticalAlignment="Stretch"
                                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}}, Path=(Validation.HasError), Converter={StaticResource BoolToVisibilityConverter}}"
                                Template="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}}, Path=ValidationErrorTemplate}" />
                        </StackPanel>

                        <Thumb x:Name="PART_TopHeaderGripper"
                            Grid.ColumnSpan="2"
                            VerticalAlignment="Top"
                            Style="{StaticResource RowHeaderGripperStyle}" />
                        <Thumb x:Name="PART_BottomHeaderGripper"
                            Grid.ColumnSpan="2"
                            VerticalAlignment="Bottom"
                            Style="{StaticResource RowHeaderGripperStyle}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsRowSelected" Value="True">
                            <Setter TargetName="ActiveRectangle" Property="Visibility"
                                Value="Visible" />
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundSelected}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushSelected}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPointerOver}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPressed}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundDisabled}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushDisabled}" />
                            <Setter Property="Foreground"
                                Value="{DynamicResource DataGridHeaderForegroundDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{DynamicResource DataGridColumnHeaderBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridHeaderBorderBrush}" />
        <Setter Property="MinHeight" Value="{StaticResource DataGridColumnHeaderMinHeight}" />
        <Setter Property="Padding" Value="{DynamicResource DataGridColumnHeaderPadding}" />
        <Setter Property="SeparatorBrush" Value="{DynamicResource DataGridHeaderSeparatorBrush}" />
        <Setter Property="SeparatorVisibility" Value="Visible" />
        <Setter Property="BorderThickness" Value="0 0 0 1" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid x:Name="RootGrid">
                        <Border x:Name="SeparatorBorder"
                            Grid.ColumnSpan="2"
                            BorderBrush="{TemplateBinding SeparatorBrush}"
                            Visibility="{TemplateBinding SeparatorVisibility}"
                            BorderThickness="0 0 1 0" />

                        <Border x:Name="HeaderBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                                <TextBlock x:Name="SortIndicator"
                                    Grid.Column="1"
                                    VerticalAlignment="Top"
                                    Visibility="Hidden"
                                    Margin="{DynamicResource DataGridSortIndicatorIconMargin}"
                                    FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                    FontSize="{DynamicResource DataGridSortIndicatorIconFontSize}"
                                    Text="&#xF090;" />

                                <Thumb x:Name="PART_LeftHeaderGripper"
                                    Grid.ColumnSpan="2"
                                    HorizontalAlignment="Left"
                                    Style="{StaticResource ColumnHeaderGripperStyle}" />
                                <Thumb x:Name="PART_RightHeaderGripper"
                                    Grid.ColumnSpan="2"
                                    HorizontalAlignment="Right"
                                    Style="{StaticResource ColumnHeaderGripperStyle}" />
                            </Grid>
                        </Border>


                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPointerOver}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPressed}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundDisabled}" />
                            <Setter Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushDisabled}" />
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortIndicator" Property="Visibility" Value="Visible" />
                            <Setter TargetName="SortIndicator" Property="Text" Value="&#xF090;" />
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortIndicator" Property="Visibility" Value="Visible" />
                            <Setter TargetName="SortIndicator" Property="Text" Value="&#xF08E;" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridColumnHeadersPresenterStyle"
        TargetType="{x:Type DataGridColumnHeadersPresenter}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeadersPresenter}">
                    <Grid>
                        <DataGridColumnHeader IsHitTestVisible="False"
                            x:Name="PART_FillerColumnHeader"
                            Background="{DynamicResource DataGridHeaderBackground}" />
                        <ItemsPresenter />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridCellsPresenterStyle" TargetType="{x:Type DataGridCellsPresenter}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCellsPresenter}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultDataGridSelectAllButtonStyle" BasedOn="{StaticResource DefaultButtonStyle}"
        TargetType="{x:Type Button}">
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Border x:Name="RootBorder"
                            Background="{DynamicResource DataGridHeaderBackground}"
                            BorderBrush="{DynamicResource DataGridHeaderBorderBrush}"
                            BorderThickness="0 0 1 1"
                            SnapsToDevicePixels="True" />
                        <TextBlock x:Name="Icon"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="4"
                            FontSize="{StaticResource DataGridSelectAllIconFontSize}"
                            Foreground="{DynamicResource DatatGridSelectAllButtonForeground}"
                            FontFamily="{DynamicResource SymbolThemeFontFamily}"
                            Text="&#xF169;" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPointerOver}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundPressed}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="RootBorder" Property="Background"
                                Value="{DynamicResource DataGridHeaderBackgroundDisabled}" />
                            <Setter TargetName="RootBorder" Property="BorderBrush"
                                Value="{DynamicResource DataGridHeaderBorderBrushDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ************************************************************** -->
    <!--              DataGrid Controls Implicit Styles                 -->
    <!-- ************************************************************** -->

    <Style x:Key="{x:Type DataGridCellsPresenter}" TargetType="{x:Type DataGridCellsPresenter}"
        BasedOn="{StaticResource DefaultDataGridCellsPresenterStyle}" />
    <Style x:Key="{x:Type DataGridColumnHeadersPresenter}"
        TargetType="{x:Type DataGridColumnHeadersPresenter}"
        BasedOn="{StaticResource DefaultDataGridColumnHeadersPresenterStyle}" />

    <!-- DataGrid Row Style -->
    <Style x:Key="{x:Type DataGridRow}" BasedOn="{StaticResource DefaultDataGridRowStyle}"
        TargetType="{x:Type DataGridRow}" />
    <Style x:Key="{x:Type DataGridCell}" BasedOn="{StaticResource DefaultDataGridCellStyle}"
        TargetType="{x:Type DataGridCell}" />

    <!-- DataGrid SelectAll Button Style -->
    <Style
        x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type DataGrid}, ResourceId=DataGridSelectAllButtonStyle}"
        BasedOn="{StaticResource DefaultDataGridSelectAllButtonStyle}" TargetType="{x:Type Button}" />

    <!-- DataGrid Row and Column Header Styles -->
    <Style x:Key="{x:Type DataGridRowHeader}" TargetType="{x:Type DataGridRowHeader}"
        BasedOn="{StaticResource DefaultDataGridRowHeaderStyle}" />
    <Style x:Key="{x:Type DataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}"
        BasedOn="{StaticResource DefaultDataGridColumnHeaderStyle}" />

    <!--DataGridColumnHeader Other Styles-->
    <Style x:Key="{x:Static DataGridColumnHeader.ColumnHeaderDropSeparatorStyleKey}"
        TargetType="{x:Type Separator}"
        BasedOn="{StaticResource DefaultDataGridHeaderDropSeparatorStyle}" />
    <Style x:Key="{x:Static DataGridColumnHeader.ColumnFloatingHeaderStyleKey}"
        TargetType="{x:Type Control}"
        BasedOn="{StaticResource DefaultDataGridColumnFloatingHeaderStyle}" />

    <!-- DataGrid Style -->
    <Style x:Key="{x:Type DataGrid}" TargetType="{x:Type DataGrid}"
        BasedOn="{StaticResource DefaultDataGridStyle}" />


</ResourceDictionary>